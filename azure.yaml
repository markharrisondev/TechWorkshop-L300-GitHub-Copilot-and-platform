# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: zavastorefrontTechWorkshop-L300-GitHub-Copilot-and-platform
metadata:
  template: zavastorefrontTechWorkshop-L300-GitHub-Copilot-and-platform

services:
  web:
    project: ./src
    language: dotnet
    host: appservice
    docker:
      path: ./src/Dockerfile
      context: ./src
